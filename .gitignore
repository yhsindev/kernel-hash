# Kernel build artifacts
*.o
*.ko
*.mod
*.mod.c
*.cmd
.tmp_versions/
Module.symvers
modules.order
*.order
*.symvers

# perf runtime artifacts
perf.data
perf.data.old

# Slidev / Node
slides/node_modules/
slides/dist/
slides/.slidev/

# Python bytecode cache
__pycache__/
*.pyc
kernel_work/

# Local-only result bucket: not published (kept off cloud)
ovs_datapath_bench/results/else/

# Large per-run dumps under results/ (regenerable; keep only small summaries)
ovs_datapath_bench/results/**/dpctl_dump_flows*.txt
ovs_datapath_bench/results/**/ofctl_dump_flows*.txt
ovs_datapath_bench/results/**/perf_report_*.txt

# OVS attack tool: compiled binary + per-run collision key dump (regenerable)
ovs_security/attack/find_jhash_collision
ovs_security/attack/keys.txt

# Part 3 attack: raw keydump debug captures (regenerable; keep templates + result summaries)
ovs_security/results/keydump_attack*.log

# Generated figures (regenerable via scripts/plot_part3.py; not tracked)
ovs_security/results/figs/

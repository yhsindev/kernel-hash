Planned install:
  bridge      : br0
  src ports   : 20000-20199  (200 values)
  dst ports   : 9000-9049  (50 values)
  output port : veth2-br
  total rules : 10000  (plus 1 NORMAL fallback)

Resolved output port:
  veth2-br -> OpenFlow port 2

[OK] 10001 rules installed on br0

Next steps:
  Inspect rules:
    sudo ovs-ofctl dump-flows br0 | head -5

  Run traffic:
    sudo DURATION=20 \
         SRCMIN=20000 SRCMAX=20199 \
         DSTMIN=9000 DSTMAX=9049 \
         ./ovs_datapath_bench/scripts/pktgen_microflows.sh

  Watch datapath:
    sudo ./ovs_datapath_bench/scripts/observe_flows.sh --watch 30

  Verify masks:
    sudo ovs-dpctl dump-flows -m | grep udp | head -30

  Restore NORMAL:
    sudo ./ovs_datapath_bench/scripts/install_irregular_udp_rules.sh restore
